"Count" - get size of list

const BearListInt& Count()

Example:

Bear::List<int> list;

list.Add(2);
list.Add(1);
list.Add(3);

for (BearListInt i = 0; i < list.Count(); i++)
{
	std::cout << list[i] << "\n";
}