"Count" - get size of list

const BearListLongInt& Count()

Example:

Bear::List<int> list;

list.Add(2);
list.Add(1);
list.Add(3);

for (BearListLongInt i = 0; i < list.Count(); i++)
{
	std::cout << list[i] << "\n";
}